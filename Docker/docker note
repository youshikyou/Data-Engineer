#interactive way in running container
docker container run --it 
#to quit gracefully without kill the terminal instead of using   exit
ctrl+P+Q


#detached and run in the background
docker contrain run -d
